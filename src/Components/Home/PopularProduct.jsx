import wait from "@/utlis/wait";

const PopularProduct = async () => {
  await wait();
  return (
    <div>
      <h2 className="text-4xl">Popular Products</h2>
    </div>
  );
};

export default PopularProduct;
